# Backend runtime + dev deps. Python 3.11+. See specs/context/tech-stack.md.

# --- runtime ---
pydantic>=2.10
pydantic-settings>=2.7
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.30
pgvector>=0.3
google-genai>=2.0          # Gemini embeddings (Lane B) + query reformulation
httpx>=0.27                # imported directly for embedding-error handling
pypdf>=5.0                 # policy-PDF text extraction
numpy>=2.0                 # L2 normalization of embeddings
scikit-learn>=1.4          # priority-classifier baseline (TF-IDF + logistic regression)

# API layer (serves the retrieval demo)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9    # PDF/file uploads

# --- Lane C (email_agent) ---
python-dotenv>=1.0

# --- dev ---
pytest>=9.1.1
ruff
