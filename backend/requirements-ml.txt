# Heavy deps for DistilBERT fine-tuning ONLY. Not needed to run the API — kept out of
# requirements.txt so CI and everyday installs stay light. Install with: make ml-deps
# CPU is fine (no GPU required); training ~959 rows takes a few minutes on CPU.
torch>=2.2
transformers>=4.40
datasets>=2.19
accelerate>=0.30
